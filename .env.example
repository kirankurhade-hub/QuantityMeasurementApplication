# QMA React Frontend — Environment Variables
# Copy this file to .env and adjust values if needed.

# API Gateway base URL (all microservice traffic routes through here)
# Default: http://localhost:8080 (matches the proxy setting in package.json)
REACT_APP_API_URL=http://localhost:8080
